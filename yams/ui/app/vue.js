export const { createApp, ref, onMounted, onBeforeUnmount, computed, watchEffect, watch, onUpdated, onBeforeMount, toRef, useTemplateRef } = Vue;
export const { createRouter, createWebHistory, useRouter, useRoute, RouterView } = VueRouter;
export const { createPinia, defineStore, storeToRefs } = Pinia;
